package DSA.LinkedList;

static class Node { // representation of a node
    int data;       // declaring variable
    Node next;      // link to next node
    Node(int d) {   // Constructor
    data = d;       //node is created with an empty reference
    next = null;
    }
}
    